# 程序化多模态源荷电网世界生成器  
## 总体目标、系统设计与分阶段实现路线

---

## 0. 项目定位

### 0.1 项目名称

**程序化多模态源荷电网世界生成器**  
英文可暂定为：

> Procedural Multimodal Source–Load–Grid World Generator

### 0.2 总体目标

构建一个可重复、可控、可扩展的合成区域世界。该世界从程序化地形出发，逐步生成水系、气候、天气、城市、产业、风电场、光伏场、负荷节点、变电站和输电线路，并通过交流潮流计算形成完整的电力系统运行状态。

最终数据应同时包含：

1. **静态地理环境模态**；
2. **动态气象模态**；
3. **城市与土地利用模态**；
4. **风电、光伏和负荷时序**；
5. **电网拓扑图**；
6. **母线、电源、负荷和支路运行状态**；
7. **潮流、越限、限发、失负荷和扩建事件**；
8. **已知的生成因果链和演化记录**。

核心生成链为：

```text
地形
→ 水系与地表属性
→ 区域气候背景
→ 动态天气过程
→ 城市和产业布局
→ 风电与光伏适宜区
→ 源荷节点与容量
→ 变电站和输电线路规划
→ 风光出力与负荷时序
→ AC 潮流 / OPF
→ 运行评价
→ 城市、电源和电网扩建
```

项目不是为了精确复现某个真实地区，而是构建一个：

- 生成逻辑明确；
- 局部物理合理；
- 模态间具有共同驱动；
- 可产生复杂空间关系；
- 可用于机器学习和因果学习验证；

的**伪物理多模态电力世界**。

---

# 1. 系统设计原则

## 1.1 单一世界坐标系

全部栅格、城市、电源、电网节点和线路共享统一二维坐标域：

\[
\Omega=[0,1]\times[0,1]
\]

或离散栅格：

\[
\Omega_{H,W}=\{0,\ldots,H-1\}\times\{0,\ldots,W-1\}
\]

所有图节点保存连续坐标：

```python
pos = [x, y]
```

所有栅格采样、场站选址和线路路径规划均基于该坐标系。

---

## 1.2 静态、快动态和慢动态分离

不要将所有变量粗暴存成单一大张量。系统状态分为三层。

### 静态地理层

\[
S\in\mathbb{R}^{C_s\times H\times W}
\]

主要包含：

- elevation；
- slope；
- aspect；
- roughness；
- river；
- lake；
- flow accumulation；
- watershed；
- flood risk；
- distance to water；
- land cover；
- buildability；
- terrain construction cost。

### 快动态气象与运行层

\[
X\in\mathbb{R}^{T\times C_d\times H\times W}
\]

主要包含：

- wind \(u,v\)；
- wind speed；
- temperature；
- humidity；
- pressure；
- cloud；
- precipitation；
- irradiance；
- weather state；
- source/load density；
- optional voltage/load-flow raster。

### 慢动态社会与基础设施层

\[
G^{(e)},\qquad e=0,\ldots,E-1
\]

其中每个演化阶段保存：

- 城市和产业分布；
- 人口和经济活动；
- 电源场站；
- 变电站；
- 输电线路；
- 容量扩建记录；
- 电网拓扑快照。

---

## 1.3 先生成共同驱动，再生成响应变量

不允许风电、光伏、负荷和天气彼此独立随机生成。

正确流程为：

```text
共同天气场
→ 各节点按位置采样
→ 风电/光伏/负荷机理映射
→ 注入电网
→ 潮流响应
```

因此，空间相邻节点应自然具有相关性，但不完全相同。

---

## 1.4 可解释生成链

每个变量应记录其主要父变量。例如：

```text
elevation → temperature
elevation + wind → orographic precipitation
wind + terrain → wind generation
irradiance + temperature → photovoltaic generation
temperature + city type → load
generation + load + grid topology → voltage and branch flow
```

生成器应能输出因果元数据，便于后续验证特征选择、Granger 因果和图关系学习。

---

## 1.5 全流程可复现

每个世界由唯一 `world_seed` 控制。

建议采用分模块随机种子：

```python
seed_terrain
seed_hydrology
seed_weather
seed_city
seed_energy
seed_grid
seed_operation
seed_evolution
```

同一配置与种子必须生成完全一致的结果。

---

# 2. 最终产物

## 2.1 世界级输出

```python
WorldData(
    config=...,
    static_maps=...,
    weather_store=...,
    evolution_states=...,
    graph_snapshots=...,
    operation_records=...,
    causal_metadata=...,
)
```

---

## 2.2 静态栅格数据

建议输出：

```python
static_maps = {
    "elevation": [H, W],
    "slope": [H, W],
    "aspect_sin": [H, W],
    "aspect_cos": [H, W],
    "roughness": [H, W],
    "river": [H, W],
    "lake": [H, W],
    "flow_accumulation": [H, W],
    "watershed_id": [H, W],
    "flood_risk": [H, W],
    "distance_to_water": [H, W],
    "land_cover": [H, W],
    "buildability": [H, W],
    "terrain_cost": [H, W],
}
```

---

## 2.3 动态气象张量

建议逻辑形状：

```python
weather: [T, C_weather, H, W]
```

初版通道：

```text
0  wind_u
1  wind_v
2  wind_speed
3  temperature
4  humidity
5  pressure
6  cloud
7  precipitation
8  irradiance
9  weather_state_continuous
```

类别天气状态可以单独保存：

```python
weather_class: [T, H, W]  # int8 / int16
```

---

## 2.4 社会与土地利用栅格

每个演化阶段保存：

```python
land_use[e] = {
    "population": [H, W],
    "economic_activity": [H, W],
    "urban": [H, W],
    "residential": [H, W],
    "commercial": [H, W],
    "industrial": [H, W],
    "agriculture": [H, W],
    "protected": [H, W],
}
```

---

## 2.5 电力异构图

建议采用 PyTorch Geometric `HeteroData`：

### 节点类型

```text
bus
generator
load
city
substation
weather_patch（可选）
```

### 边类型

```text
bus --branch--> bus
generator --connects_to--> bus
load --connects_to--> bus
city --served_by--> substation
substation --maps_to--> bus
weather_patch --affects--> generator
weather_patch --affects--> load
```

---

## 2.6 运行输出

每个时间点至少保存：

```text
generator P/Q
load P/Q
bus voltage magnitude
bus voltage angle
branch active flow
branch reactive flow
branch loading rate
system loss
slack output
curtailment
load shedding
power-flow convergence flag
constraint violation flags
```

---

## 2.7 演化事件

```python
events = [
    {
        "stage": 2,
        "type": "add_wind_farm",
        "location": [x, y],
        "capacity_mw": 120.0,
        "reason": "...",
    },
    {
        "stage": 3,
        "type": "add_transmission_line",
        "from_bus": ...,
        "to_bus": ...,
        "reason": "persistent congestion",
    },
]
```

---

# 3. 推荐工程目录

```text
world_generator/
├─ configs/
│  ├─ default_world.yaml
│  ├─ small_debug.yaml
│  └─ large_experiment.yaml
│
├─ core/
│  ├─ config.py
│  ├─ random_state.py
│  ├─ coordinates.py
│  ├─ datatypes.py
│  └─ world.py
│
├─ terrain/
│  ├─ noise.py
│  ├─ terrain_generator.py
│  ├─ erosion.py
│  ├─ derivatives.py
│  └─ visualization.py
│
├─ hydrology/
│  ├─ flow_direction.py
│  ├─ flow_accumulation.py
│  ├─ river_generator.py
│  ├─ lake_generator.py
│  └─ flood_risk.py
│
├─ climate/
│  ├─ baseline_climate.py
│  ├─ wind_field.py
│  ├─ temperature_field.py
│  ├─ moisture_field.py
│  ├─ precipitation_field.py
│  ├─ irradiance_field.py
│  └─ weather_simulator.py
│
├─ society/
│  ├─ suitability.py
│  ├─ city_generator.py
│  ├─ land_use.py
│  ├─ population.py
│  ├─ economy.py
│  └─ growth.py
│
├─ energy/
│  ├─ wind_suitability.py
│  ├─ pv_suitability.py
│  ├─ wind_farm.py
│  ├─ pv_farm.py
│  ├─ conventional_generation.py
│  ├─ storage.py
│  └─ siting.py
│
├─ demand/
│  ├─ load_types.py
│  ├─ base_profiles.py
│  ├─ weather_response.py
│  ├─ load_generator.py
│  └─ reactive_power.py
│
├─ grid/
│  ├─ node_builder.py
│  ├─ candidate_edges.py
│  ├─ routing_cost.py
│  ├─ line_routing.py
│  ├─ topology_builder.py
│  ├─ electrical_parameters.py
│  └─ heterodata_builder.py
│
├─ powerflow/
│  ├─ case_builder.py
│  ├─ dispatch.py
│  ├─ ac_power_flow.py
│  ├─ opf.py
│  ├─ recovery.py
│  └─ metrics.py
│
├─ evolution/
│  ├─ planner.py
│  ├─ triggers.py
│  ├─ candidate_actions.py
│  ├─ scoring.py
│  └─ simulator.py
│
├─ dataset/
│  ├─ sample_builder.py
│  ├─ temporal_windows.py
│  ├─ split.py
│  ├─ normalization.py
│  ├─ zarr_writer.py
│  └─ reader.py
│
├─ visualization/
│  ├─ map_plot.py
│  ├─ weather_animation.py
│  ├─ grid_plot.py
│  ├─ evolution_plot.py
│  └─ dashboard.py
│
├─ tests/
│  ├─ test_terrain.py
│  ├─ test_weather.py
│  ├─ test_siting.py
│  ├─ test_grid.py
│  ├─ test_powerflow.py
│  └─ test_reproducibility.py
│
└─ scripts/
   ├─ generate_world.py
   ├─ run_operation.py
   ├─ run_evolution.py
   ├─ export_dataset.py
   └─ visualize_world.py
```

---

# 4. 分阶段实现路线

---

# 阶段 0：配置系统、数据类型与随机状态

## 目标

建立所有后续模块共用的配置、坐标、数据结构和随机数管理机制。

## 输入

- YAML 配置；
- `world_seed`；
- 栅格尺寸；
- 时间参数。

## 主要任务

1. 定义 `WorldConfig`；
2. 定义 `TerrainState`、`WeatherState`、`SocietyState`、`GridState`；
3. 建立分模块随机数生成器；
4. 定义统一坐标转换；
5. 定义结果保存路径；
6. 定义版本信息和配置快照。

## 推荐接口

```python
config = load_world_config("configs/small_debug.yaml")
rngs = build_rng_registry(config.seed)
world = WorldState.empty(config)
```

## 验收标准

- 同一配置和种子重复运行结果一致；
- 不同模块随机性相互隔离；
- 可以正确进行 grid coordinate 与 normalized coordinate 转换；
- 配置可序列化为 JSON/YAML；
- 所有数组 dtype 和 shape 有明确约定。

---

# 阶段 1：基础程序化地形生成

## 目标

生成具有山脉、丘陵、平原、盆地和边界过渡的高程图。

## 输入

```text
H, W
terrain seed
octaves
mountain ridge count
basin count
height range
```

## 生成方法

基础地形：

\[
E_0(x,y)=\sum_{k=1}^{K}a_kN(f_kx,f_ky)
\]

再叠加：

- 山脉距离场；
- 高原；
- 盆地；
- 边界衰减；
- 可选海域掩码。

## 输出

```python
TerrainBase(
    elevation=[H, W],
    land_mask=[H, W],
)
```

## 建议功能

- 多尺度 OpenSimplex/Perlin 噪声；
- 可控山脉方向；
- 可控平原比例；
- 可控海拔范围；
- 高程归一化与物理单位映射。

## 可视化

- 高程热图；
- 等高线；
- 3D surface；
- 高程直方图。

## 验收标准

- 无 NaN/Inf；
- 高程范围满足配置；
- 不同种子产生明显不同地形；
- 相同种子完全复现；
- 地形中同时存在低频大结构和高频局部结构；
- 平原、山地比例可被配置控制。

---

# 阶段 2：地形派生属性与侵蚀

## 目标

从高程图生成坡度、坡向、粗糙度，并通过简化侵蚀提高地貌合理性。

## 输入

- elevation；
- cell size；
- erosion parameters。

## 主要任务

1. 计算坡度；
2. 计算坡向；
3. 计算局部起伏度；
4. 计算曲率；
5. 执行可选热侵蚀；
6. 执行可选简化水力侵蚀。

## 输出

```python
TerrainFeatures(
    elevation=[H, W],
    slope=[H, W],
    aspect_sin=[H, W],
    aspect_cos=[H, W],
    roughness=[H, W],
    curvature=[H, W],
)
```

## 验收标准

- 平坦区域坡度接近零；
- 山脊与谷地具有明显曲率差异；
- 坡向使用 `sin/cos`，避免角度不连续；
- 侵蚀前后总体地形结构保持，局部过渡更自然；
- 数值范围稳定。

---

# 阶段 3：水文、水系与洪水风险

## 目标

根据地形生成汇流方向、汇流量、河流、湖泊和流域。

## 输入

- elevation；
- precipitation baseline；
- river thresholds。

## 主要任务

1. 填洼或允许部分自然湖泊；
2. 计算流向；
3. 计算汇流量；
4. 根据阈值提取河网；
5. 生成湖泊；
6. 生成流域编号；
7. 计算距水体距离；
8. 估计洪水风险。

## 输出

```python
HydrologyState(
    flow_direction=[H, W],
    flow_accumulation=[H, W],
    river=[H, W],
    lake=[H, W],
    watershed_id=[H, W],
    distance_to_water=[H, W],
    flood_risk=[H, W],
)
```

## 验收标准

- 河流沿高处流向低处；
- 主河道具有连续性；
- 高汇流量区域与河道一致；
- 湖泊位于洼地；
- 河流不应大面积逆坡；
- 洪水风险主要分布在低洼河谷和湖泊周边。

---

# 阶段 4：静态土地属性与建设成本

## 目标

生成后续城市、电源和线路选址需要的静态约束图。

## 输入

- terrain features；
- hydrology state；
- 随机资源场；
- 保护区配置。

## 主要任务

生成：

- land cover；
- soil/resource quality；
- vegetation；
- protected area；
- construction difficulty；
- buildability；
- road/line terrain cost。

## 示例建设成本

\[
C_{\mathrm{terrain}}
=
w_s\cdot\text{slope}
+
w_r\cdot\text{roughness}
+
w_w\cdot\text{water crossing}
+
w_p\cdot\text{protected area}
\]

## 输出

```python
StaticLandState(
    land_cover=[H, W],
    vegetation=[H, W],
    protected=[H, W],
    buildability=[H, W],
    terrain_cost=[H, W],
)
```

## 验收标准

- 水体和保护区不可建设；
- 高坡度与高粗糙度区域建设成本高；
- 平原区域适宜建设；
- 所有成本图归一化且含义一致；
- 可直接供 A* 路径规划使用。

---

# 阶段 5：区域气候背景

## 目标

生成不随小时快速变化的气候基线。

## 输入

- elevation；
- latitude proxy；
- distance to water；
- terrain orientation；
- climate zone config。

## 主要任务

生成：

- 年平均温度；
- 年温差；
- 基础湿度；
- 主导风向；
- 主导风速；
- 年平均云量；
- 年平均降水；
- 年平均辐照度。

## 基础温度

\[
T_{\mathrm{base}}(x,y)
=
T_{\mathrm{lat}}(y)
-
\gamma_EE(x,y)
+
T_{\mathrm{water}}(x,y)
\]

## 输出

```python
ClimateBaseline(
    mean_temperature=[H, W],
    annual_temperature_amplitude=[H, W],
    mean_humidity=[H, W],
    prevailing_wind_u=[H, W],
    prevailing_wind_v=[H, W],
    mean_precipitation=[H, W],
    mean_cloud=[H, W],
    mean_irradiance=[H, W],
)
```

## 验收标准

- 高海拔区域平均温度更低；
- 水体附近温度波动较小；
- 迎风坡降水基线高于背风坡；
- 不同区域存在可辨识气候差异；
- 气候背景空间连续。

---

# 阶段 6：动态天气模拟器

## 目标

生成连续、平滑、可传播的 `T × C × H × W` 气象张量。

## 输入

- climate baseline；
- terrain；
- hydrology；
- start time；
- time step；
- simulation length；
- weather seed。

## 主要过程

### 6.1 季节周期

\[
T_{\mathrm{season}}(t)
=
A_{\mathrm{year}}\sin(2\pi d_t/D+\phi)
\]

### 6.2 日周期

\[
T_{\mathrm{day}}(t)
=
A_{\mathrm{day}}\sin(2\pi h_t/24+\psi)
\]

### 6.3 平流天气系统

使用移动的平滑随机场：

\[
Z_{t+1}(x,y)
=
\rho Z_t(x-\Delta x_t,y-\Delta y_t)
+
\sqrt{1-\rho^2}\epsilon_t(x,y)
\]

### 6.4 地形修正风场

考虑：

- 山脊加速；
- 谷地导流；
- 地表粗糙度减速；
- 背风侧减速；
- 局部扰动。

### 6.5 地形降雨

\[
R_{\mathrm{oro}}
\propto
\max(\boldsymbol V\cdot\nabla E,0)
\]

### 6.6 辐照度

由太阳周期、云量、降雨、坡度和坡向共同决定。

## 输出

```python
WeatherStore(
    dynamic=[T, C_weather, H, W],
    weather_class=[T, H, W],
    timestamps=[T],
    channel_names=[...],
)
```

## 推荐保存

使用 Zarr：

```text
chunks = [time_chunk, channel_chunk, H, W]
```

初版推荐：

```text
time_chunk = 24 或 168
channel_chunk = 全部通道
```

## 验收标准

- 相邻时刻天气连续；
- 天气系统能够空间移动；
- 风速、降雨、云量和辐照度之间关系合理；
- 夜间辐照度接近零；
- 云量和降雨增加时辐照度下降；
- 高山温度通常低于平原；
- 迎风坡降雨总体高于背风坡；
- 无异常跳变、NaN 或明显边界伪影。

---

# 阶段 7：城市适宜度与初始城市生成

## 目标

根据地形、水源、气候和资源生成城市位置，而非随机放置负荷。

## 输入

- static land state；
- climate baseline；
- water distance；
- resource maps；
- target city count。

## 城市适宜度

\[
S_{\mathrm{city}}
=
w_fS_{\mathrm{flat}}
+
w_wS_{\mathrm{water}}
+
w_cS_{\mathrm{climate}}
+
w_rS_{\mathrm{resource}}
-
w_{\mathrm{flood}}R_{\mathrm{flood}}
-
w_pR_{\mathrm{protected}}
\]

## 主要任务

1. 计算城市适宜度图；
2. 对高适宜区做非极大值抑制；
3. 控制城市间最小距离；
4. 采样城市中心；
5. 分配城市规模；
6. 生成城市影响半径；
7. 生成初始人口和经济活动。

## 输出

```python
CityState(
    cities=[CityNode, ...],
    suitability=[H, W],
    population=[H, W],
    economic_activity=[H, W],
    urban_density=[H, W],
)
```

## 验收标准

- 城市主要位于平缓、可建设区域；
- 城市接近水源但不落入高洪水风险区；
- 城市之间存在合理间距；
- 城市规模存在层级差异；
- 总人口和城市数量满足配置。

---

# 阶段 8：土地利用与负荷区域划分

## 目标

将城市扩展为住宅、商业、工业等负荷区域。

## 输入

- city state；
- terrain；
- water；
- transport proxy；
- resource maps。

## 主要任务

1. 生成住宅区；
2. 生成商业中心；
3. 生成工业区；
4. 保留农业和自然区域；
5. 分配城市类型；
6. 分配负荷类型；
7. 生成负荷容量密度图。

## 输出

```python
LandUseState(
    residential=[H, W],
    commercial=[H, W],
    industrial=[H, W],
    agriculture=[H, W],
    load_capacity_density=[H, W],
)
```

## 验收标准

- 商业区接近城市中心；
- 住宅区环绕商业中心扩展；
- 工业区偏向城市边缘、交通便利且较平坦区域；
- 不在湖泊、河道和保护区建设；
- 土地利用类别无大面积重叠冲突。

---

# 阶段 9：风电和光伏适宜度

## 目标

根据长期天气和建设条件生成可解释的新能源选址评分。

## 风电适宜度

输入因素：

- 平均风速；
- 风功率密度；
- 风速稳定性；
- 风向集中度；
- 山脊增速；
- 湍流风险；
- 坡度；
- 保护区；
- 距城市安全距离；
- 初步并网距离。

输出：

```python
wind_suitability: [H, W]
```

## 光伏适宜度

输入因素：

- 平均辐照度；
- 云量；
- 温度；
- 坡度；
- 坡向；
- 地表类型；
- 保护区；
- 城市占用；
- 初步并网距离。

输出：

```python
pv_suitability: [H, W]
```

## 验收标准

- 风电高适宜区对应高风资源；
- 光伏高适宜区对应高辐照、低云量和平缓地形；
- 水体、城市核心和保护区不可选；
- 适宜度各因素可单独可视化；
- 权重可通过配置调整。

---

# 阶段 10：新能源场站选址和容量分配

## 目标

从适宜度图中选择风电场与光伏场位置，并生成场站级节点。

## 输入

- suitability maps；
- target renewable penetration；
- minimum spacing；
- buildable mask；
- capacity ranges。

## 主要任务

1. 候选区域连通域提取；
2. 区域评分；
3. 非极大值抑制；
4. 场站中心选择；
5. 场站边界生成；
6. 容量分配；
7. 风机/光伏阵列子节点可选生成；
8. 记录预计年利用小时。

## 输出

```python
RenewableState(
    wind_farms=[WindFarm, ...],
    pv_farms=[PVFarm, ...],
    wind_capacity_map=[H, W],
    pv_capacity_map=[H, W],
)
```

## 验收标准

- 场站不互相严重重叠；
- 场站位于高适宜度区域；
- 容量与区域面积及资源水平相关；
- 总装机满足目标范围；
- 风电和光伏具有明显不同空间偏好。

---

# 阶段 11：负荷节点与源节点抽象

## 目标

将连续土地利用和场站映射为离散电力系统节点。

## 输入

- city and land-use state；
- renewable state；
- load density；
- target bus count。

## 主要任务

1. 对城市负荷密度做聚类；
2. 生成 load nodes；
3. 生成 generator nodes；
4. 生成候选 substation nodes；
5. 建立节点坐标；
6. 分配额定容量；
7. 分配节点类型；
8. 生成初始母线候选。

## 输出

```python
SpatialPowerNodes(
    cities=[...],
    loads=[...],
    generators=[...],
    substations=[...],
)
```

## 验收标准

- 离散节点能够较好覆盖负荷密度；
- 节点容量总和与栅格容量总和一致；
- 场站与发电节点一一对应或按配置拆分；
- 所有节点位置合法；
- 节点具有稳定唯一 ID。

---

# 阶段 12：候选线路、最小成本路径与初始电网

## 目标

根据节点位置和地形成本生成空间合理、连通且具有冗余的电网。

## 输入

- power nodes；
- terrain cost；
- river/lake；
- protected areas；
- topology parameters。

## 主要任务

### 12.1 候选边

使用：

- k-nearest neighbors；
- Delaunay triangulation；
- distance threshold；

生成候选连接。

### 12.2 路径成本

\[
C_{\mathrm{path}}
=
w_dC_{\mathrm{distance}}
+
w_sC_{\mathrm{slope}}
+
w_wC_{\mathrm{water}}
+
w_pC_{\mathrm{protected}}
+
w_uC_{\mathrm{urban}}
\]

### 12.3 实际线路路径

使用 A* 或 Dijkstra 在栅格成本图上寻找最低成本路径。

### 12.4 初始拓扑

1. 最小生成树保证连通；
2. 添加高价值冗余边；
3. 对大城市形成局部环网；
4. 连接远端新能源基地；
5. 设置 slack/主电源区域。

## 输出

```python
GridSpatialState(
    buses=[...],
    branches=[...],
    line_paths=[...],
    topology=...,
)
```

## 验收标准

- 全网连通；
- 无孤立母线；
- 线路尽量避开高成本地形；
- 主负荷中心具有一定冗余；
- 电源和负荷均连接到电网；
- 拓扑规模满足配置。

---

# 阶段 13：电气参数生成

## 目标

为母线、支路、电源和负荷分配可用于潮流计算的电气参数。

## 输入

- spatial grid；
- line length；
- voltage level rules；
- node capacities。

## 主要任务

1. 根据距离生成线路长度；
2. 根据线路类型生成 \(r,x,b\)；
3. 设置热稳定容量；
4. 设置母线类型；
5. 设置电压等级；
6. 设置变压器；
7. 设置发电机 P/Q 上下限；
8. 设置负荷功率因数；
9. 设置 slack generator。

## 输出

可转换为：

- PYPOWER/MATPOWER case；
- pandapower network；
- PF-Delta 所需 HeteroData。

## 验收标准

- 初始网络能够在基准源荷下收敛；
- 参数无非法值；
- 支路容量与所服务负荷规模匹配；
- slack 容量足够；
- 标幺制转换正确。

---

# 阶段 14：风电、光伏和负荷时序生成

## 目标

基于动态天气和节点位置生成源荷序列。

## 14.1 风电

节点按位置采样：

```text
wind_u
wind_v
wind_speed
temperature
```

再通过功率曲线、空气密度、可用率、限功率生成：

```python
P_wind[t, n_wind]
Q_wind[t, n_wind]
```

## 14.2 光伏

节点采样：

```text
irradiance
temperature
cloud
precipitation
```

再考虑：

- 电池温度；
- 温度系数；
- 逆变器效率；
- 设备可用率；
- 倾角和朝向。

输出：

```python
P_pv[t, n_pv]
Q_pv[t, n_pv]
```

## 14.3 负荷

负荷由以下因素共同决定：

- 城市类型；
- 住宅/商业/工业类别；
- 日周期；
- 周周期；
- 节假日；
- 温度；
- 湿度；
- 天气状态；
- 区域共同扰动；
- 节点个体扰动。

输出：

```python
P_load[t, n_load]
Q_load[t, n_load]
```

## 验收标准

- 风电与局部风速高度相关但非完全确定；
- 光伏夜间接近零；
- 云量和降雨会降低光伏；
- 负荷具有合理日内曲线；
- 高温或低温时负荷上升；
- 相邻节点具有共同波动；
- 不同节点保留异质性；
- 总源荷规模与电网容量匹配。

---

# 阶段 15：调度、AC 潮流与异常恢复

## 目标

将每个时刻的源荷写入电网并求解物理状态。

## 输入

- grid case；
- generator P/Q；
- load P/Q；
- dispatch rules。

## 主要任务

1. 写入风光出力；
2. 写入负荷；
3. 常规机组经济或比例调度；
4. slack 平衡；
5. 执行 AC 潮流；
6. 检查电压和线路越限；
7. 若失败，执行恢复策略。

## 恢复策略顺序

```text
常规机组再调度
→ 储能充放电
→ 无功调整
→ 新能源限发
→ 负荷削减
→ 样本标记为不可行
```

## 输出

```python
OperationRecord(
    converged=...,
    bus_vm=...,
    bus_va=...,
    branch_p=...,
    branch_q=...,
    branch_loading=...,
    generation=...,
    load=...,
    curtailment=...,
    load_shedding=...,
    losses=...,
)
```

## 验收标准

- 大多数时刻正常收敛；
- 功率平衡误差小于阈值；
- 不收敛时有明确恢复记录；
- 限发和失负荷可追溯；
- 所有运行量与节点 ID 对齐；
- 结果可直接转换为 PF-Delta 数据。

---

# 阶段 16：慢时间尺度城市与电网演化

## 目标

根据多年运行统计推动城市、电源和电网扩建。

## 输入

每个阶段的统计：

- 人口增长；
- 负荷增长；
- 平均风光资源；
- 弃风弃光率；
- 线路拥塞率；
- 电压越限率；
- 失负荷率；
- 供电可靠性；
- 建设成本。

## 主要触发条件

### 城市扩张

- 人口增长；
- 经济活动增长；
- 可建设土地充足；
- 交通和电力供应良好。

### 新增电源

- 区域能源缺口；
- 高可再生资源；
- 低建设成本；
- 并网能力允许。

### 新增线路

- 长期拥塞；
- 高失负荷概率；
- 新城市或场站未充分接入；
- N-1 冗余不足。

### 新增储能

- 风光波动大；
- 限发率高；
- 峰谷差大；
- 局部线路拥塞。

## 候选动作评分

\[
J(a)
=
w_1\Delta\text{Reliability}
+
w_2\Delta\text{Renewable Utilization}
+
w_3\Delta\text{Voltage Security}
-
w_4\text{Construction Cost}
-
w_5\text{Environmental Cost}
\]

## 输出

```python
EvolutionState(
    stage=e,
    society_state=...,
    energy_state=...,
    grid_state=...,
    events=[...],
)
```

## 验收标准

- 演化由运行统计触发，而非纯随机；
- 扩建后拥塞或失负荷应有所改善；
- 城市增长会带来负荷增长；
- 新能源扩建会改变潮流分布；
- 每个演化动作有原因、成本和效果记录；
- 可以重放整个演化历史。

---

# 阶段 17：多模态数据集构建

## 目标

将世界状态转换为机器学习可用的数据集。

## 输入窗口

\[
[t-L+1,\ldots,t]
\]

## 预测窗口

\[
[t+1,\ldots,t+H]
\]

## 输入模态

### 静态栅格

```text
elevation
slope
aspect
river
land cover
population
urban/industrial
wind suitability
pv suitability
```

### 动态栅格

```text
wind_u/v
temperature
humidity
cloud
precipitation
irradiance
```

### 图结构

```text
bus
generator
load
branch
relative positions
electrical parameters
historical operation states
```

## 输出任务

可配置为：

- wind P；
- PV P；
- load P；
- load Q；
- bus voltage；
- branch flow；
- congestion probability；
- curtailment；
- load shedding；
- topology evolution action。

## 推荐样本格式

```python
sample = {
    "static_maps": ...,
    "weather_history": ...,
    "graph_history": ...,
    "time_features": ...,
    "targets": ...,
    "masks": ...,
    "metadata": ...,
}
```

## 划分原则

按连续时间段划分，不随机打乱全部时刻：

```text
train → earlier periods
valid → middle-late periods
test  → final periods or unseen worlds
```

同时建议增加：

- unseen weather test；
- unseen topology test；
- unseen world seed test；
- high renewable penetration test；
- extreme weather test。

## 验收标准

- 不存在未来信息泄漏；
- 所有模态时间对齐；
- 图节点和输出索引一致；
- 归一化只使用训练集统计量；
- 可按时间窗口高效读取；
- 支持单世界和多世界训练。

---

# 阶段 18：验证、可视化与基准实验

## 目标

建立完整质量控制体系。

## 必须可视化

1. 高程、坡度和水系；
2. 气温、风场、云量、降雨和辐照度；
3. 城市、人口和土地利用；
4. 风电/光伏适宜度；
5. 场站位置；
6. 电网节点和线路；
7. 线路实际地形路径；
8. 源荷日曲线；
9. 电压和潮流分布；
10. 多阶段城市与电网演化。

## 必须统计

### 地形

- 高程分布；
- 坡度分布；
- 平原/山地比例；
- 河流长度。

### 天气

- 时间自相关；
- 空间相关长度；
- 风速分布；
- 降雨零值率；
- 温度季节性；
- 辐照度昼夜规律。

### 源荷

- 容量因子；
- 日内峰谷；
- 节点相关性；
- ramp rate；
- 风光与天气相关性；
- 负荷与温度关系。

### 电网

- 潮流收敛率；
- 电压越限率；
- 支路过载率；
- 网损率；
- 弃风弃光率；
- 失负荷率；
- slack 出力分布。

## 基准模型

建议至少提供：

- persistence；
- MLP；
- LSTM/GRU；
- CNN weather encoder；
- GCN/GAT；
- weather CNN + grid GNN；
- PF-Delta baseline；
- 多模态门控模型。

---

# 5. 配置文件建议

```yaml
seed: 42

world:
  height: 64
  width: 64
  cell_size_km: 2.0

time:
  step_minutes: 60
  operation_days_per_stage: 365
  evolution_stages: 5

terrain:
  octaves: 6
  mountain_ridges: 4
  basins: 2
  erosion_steps: 20
  elevation_min_m: 0
  elevation_max_m: 2500

hydrology:
  river_threshold_quantile: 0.96
  allow_lakes: true

weather:
  channels:
    - wind_u
    - wind_v
    - wind_speed
    - temperature
    - humidity
    - pressure
    - cloud
    - precipitation
    - irradiance
  temporal_rho: 0.95
  weather_system_count: 6

society:
  city_count: 8
  total_population: 1000000
  min_city_distance_cells: 8
  industrial_ratio: 0.25

energy:
  wind_farm_count: 8
  pv_farm_count: 8
  renewable_penetration_target: 0.45
  wind_capacity_range_mw: [30, 200]
  pv_capacity_range_mw: [20, 150]

grid:
  target_bus_count: 80
  candidate_knn: 6
  redundancy_ratio: 0.20
  use_astar_routing: true

powerflow:
  solver: pandapower
  enable_opf: false
  max_voltage_pu: 1.05
  min_voltage_pu: 0.95
  max_branch_loading_percent: 100

dataset:
  history_steps: 24
  forecast_steps: 6
  storage: zarr
  dtype: float32
```

---

# 6. 核心接口建议

## 6.1 顶层生成器

```python
class ProceduralWorldGenerator:
    def __init__(self, config: WorldConfig):
        ...

    def generate_static_world(self) -> WorldState:
        ...

    def simulate_weather(self, world: WorldState) -> WeatherStore:
        ...

    def generate_society(self, world: WorldState) -> SocietyState:
        ...

    def site_energy_assets(self, world: WorldState) -> EnergyState:
        ...

    def build_grid(self, world: WorldState) -> GridState:
        ...

    def simulate_operation(self, world: WorldState) -> OperationStore:
        ...

    def evolve(self, world: WorldState) -> WorldState:
        ...

    def export_dataset(self, world: WorldState, output_dir: str):
        ...
```

## 6.2 通用栅格采样

```python
def sample_raster_at_positions(
    raster: torch.Tensor,
    positions: torch.Tensor,
    mode: str = "bilinear",
) -> torch.Tensor:
    # raster: [C, H, W] or [T, C, H, W]
    # positions: [N, 2], normalized xy in [0, 1]
    ...
```

## 6.3 适宜度组合

```python
def weighted_suitability(
    positive_factors: dict[str, torch.Tensor],
    negative_factors: dict[str, torch.Tensor],
    weights: dict[str, float],
    valid_mask: torch.Tensor,
) -> torch.Tensor:
    ...
```

## 6.4 线路路径

```python
def route_line(
    start_xy: tuple[int, int],
    end_xy: tuple[int, int],
    cost_map: np.ndarray,
    forbidden_mask: np.ndarray,
) -> list[tuple[int, int]]:
    ...
```

## 6.5 潮流输入构造

```python
def build_powerflow_case(
    grid_state: GridState,
    generation_pq: dict,
    load_pq: dict,
):
    ...
```

---

# 7. 分阶段交付与 Codex 实现顺序

每次只实现一个可独立验收的模块。

## 第 1 批：世界基础

1. 配置、随机状态和数据类型；
2. 程序化高程；
3. 坡度、坡向和粗糙度；
4. 水系和洪水风险；
5. 静态地图可视化。

### 第一批完成标志

能够输入一个 seed，输出一组可重复的静态区域地图。

---

## 第 2 批：天气系统

6. 区域气候基线；
7. 动态温度；
8. 动态风场；
9. 云量、湿度和降雨；
10. 辐照度；
11. TCHW 存储和天气动画。

### 第二批完成标志

能够生成一个月或一年的连续天气张量，并通过动画观察天气系统传播。

---

## 第 3 批：城市与土地利用

12. 城市适宜度；
13. 城市中心生成；
14. 人口和经济活动；
15. 住宅、商业、工业布局；
16. 负荷容量密度图。

### 第三批完成标志

能够由地形和气候自动生成具有层级的城市与负荷空间分布。

---

## 第 4 批：新能源场站

17. 风电适宜度；
18. 光伏适宜度；
19. 风电场选址；
20. 光伏场选址；
21. 场站容量分配。

### 第四批完成标志

能够解释每个场站为什么建立在当前位置，并输出适宜度与建设成本。

---

## 第 5 批：电网空间规划

22. 源荷节点抽象；
23. 变电站选址；
24. 候选线路；
25. A* 最小成本路径；
26. MST 主干网；
27. 冗余边和局部环网；
28. 电气参数。

### 第五批完成标志

能够生成一个空间上合理、拓扑连通、参数完整的初始电网。

---

## 第 6 批：源荷与潮流

29. 风电出力；
30. 光伏出力；
31. 负荷 P/Q；
32. 常规机组调度；
33. AC 潮流；
34. 越限检测；
35. 恢复策略；
36. PF-Delta 数据转换。

### 第六批完成标志

能够运行连续源荷序列，并输出有效的电压和支路潮流状态。

---

## 第 7 批：演化

37. 城市增长；
38. 负荷增长；
39. 拥塞统计；
40. 扩建触发；
41. 新线路与新电源候选；
42. 扩建动作评分；
43. 多阶段仿真；
44. 演化可视化。

### 第七批完成标志

能够看到城市、负荷、新能源和电网在多个规划阶段共同演化。

---

## 第 8 批：数据集与模型接口

45. Zarr/HDF5 数据存储；
46. HeteroData 构造；
47. 时间窗口样本；
48. 训练/验证/测试划分；
49. 归一化；
50. 数据读取器；
51. 基准模型；
52. 质量报告。

---

# 8. 最小可行版本

## MVP-1：静态世界

```text
64×64 地形
+ 水系
+ 城市
+ 风电/光伏适宜度
```

不生成时间序列，不做潮流。

## MVP-2：动态天气与源荷

```text
静态世界
+ 30 天小时级天气
+ 风电/光伏/负荷时序
```

不构建复杂电网。

## MVP-3：初始电网与潮流

```text
源荷节点
+ 初始线路
+ AC 潮流
+ PF-Delta 图数据
```

## MVP-4：完整一年

```text
1 年小时级天气
+ 源荷
+ 潮流
+ 数据集导出
```

## MVP-5：多阶段演化

```text
5 个规划阶段
+ 城市增长
+ 电源扩建
+ 电网扩建
```

---

# 9. 推荐初始规模

## 调试配置

```text
H × W             = 32 × 32
时间步长           = 1 h
运行天数           = 30
城市数量           = 4
风电场             = 3
光伏场             = 3
母线数量           = 20–30
演化阶段           = 1
```

## 正式初版

```text
H × W             = 64 × 64
时间步长           = 1 h
每阶段运行长度      = 365 d
城市数量           = 5–10
风电场             = 5–15
光伏场             = 5–15
母线数量           = 50–120
负荷节点           = 30–100
演化阶段           = 5
```

## 后续扩展

```text
128 × 128 或更大地图
15 min 时间分辨率
多气候区
极端天气
储能与需求响应
N-1 故障
OPF 与扩建优化
多世界并行生成
```

---

# 10. 关键风险与控制策略

## 10.1 模拟过度复杂

风险：一开始同时实现天气、城市、选址、电网和潮流，难以定位问题。

控制：

- 严格按阶段实现；
- 每个阶段独立保存中间结果；
- 每个模块有单元测试；
- MVP 完成后再增加物理细节。

## 10.2 生成数据过于简单

风险：风电完全由风速单一公式决定，模型容易直接拟合。

控制：

- 加入空间平滑扰动；
- 加入设备异质性；
- 加入限发、故障和测量噪声；
- 加入共同驱动与局部扰动；
- 加入天气传播和滞后效应。

## 10.3 生成数据缺乏一致性

风险：云量、降雨和辐照度互相矛盾。

控制：

- 从少量潜在天气状态生成多个观测变量；
- 明确变量依赖顺序；
- 加入一致性检查；
- 不独立随机采样高度相关模态。

## 10.4 潮流频繁不收敛

控制：

- 初始容量留足裕度；
- 逐步增加新能源渗透率；
- 使用常规机组和 slack 调节；
- 设置恢复策略；
- 将不可行事件保存为标签，而非全部丢弃。

## 10.5 数据规模过大

控制：

- 静态地图不沿时间重复；
- 动态数据使用 Zarr 分块；
- 类别图使用整数；
- 掩码使用 bool；
- 只在拓扑变化时保存图快照；
- 潮流时序按数组保存，不重复序列化完整 HeteroData。

## 10.6 演化行为失控

控制：

- 每阶段限制最多动作数；
- 每个动作必须通过成本收益评分；
- 扩建后重新运行潮流；
- 若效果不改善则撤销动作；
- 保存规划前后指标差异。

---

# 11. 成功判定

项目完成后，应满足以下条件。

## 地理层

- 地形、水系和城市位置具有合理空间关系；
- 城市和新能源选址可被适宜度解释；
- 线路能够避开高成本地形。

## 气象层

- 天气具有时间连续性和空间传播；
- 地形能够影响温度、风和降雨；
- 各气象变量之间基本一致。

## 源荷层

- 风电、光伏和负荷由共同天气驱动；
- 不同节点具有空间相关和个体差异；
- 源荷曲线符合基本运行规律。

## 电网层

- 初始网络连通；
- 潮流大部分时刻收敛；
- 运行状态能够反映源荷变化；
- 越限、限发和失负荷事件可追踪。

## 演化层

- 城市增长能够推动负荷增长；
- 资源与需求能够推动电源建设；
- 拥塞和可靠性能够推动线路扩建；
- 扩建后运行指标有可解释变化。

## 数据集层

- 数据能够直接用于多模态预测、GNN、因果学习和潮流计算；
- 每个样本具有明确时间、空间和节点身份；
- 支持跨世界、跨拓扑和极端场景测试；
- 生成过程和真实依赖关系可查询。

---

# 12. 最终研究价值

该系统最终形成的不只是源荷预测数据，而是一套包含以下五类关系的多模态实验环境：

1. **地理关系**：地形、水系、土地与建设条件；
2. **气象关系**：风、温度、湿度、云量、降雨和辐照；
3. **社会关系**：城市、人口、产业和负荷；
4. **能源关系**：风电、光伏、常规电源和储能；
5. **电网关系**：母线、支路、潮流、拥塞和扩建。

其核心研究优势是：

> 所有模态来源于同一程序化世界，变量之间既存在共享驱动，又保留结构差异；同时完整生成链已知，因此可以同时用于预测精度、图结构学习、特征选择、因果识别、鲁棒性和物理一致性验证。

---

# 13. Codex 单步实现任务模板

后续每次向 Codex 下发任务时，建议统一使用以下模板：

```markdown
## 当前任务

实现：`模块名称`

## 已有输入

- 输入文件：
- 依赖模块：
- 输入数据结构：
- 配置字段：

## 功能要求

1.
2.
3.

## 输出接口

```python
# 明确函数或类签名
```

## 数据形状

- 输入：
- 输出：
- dtype：
- device：

## 约束

- 不允许：
- 必须：
- 随机性：
- 性能要求：

## 验收标准

1.
2.
3.

## 测试

- 单元测试：
- 可视化测试：
- 边界条件：
```

每个模块完成后，必须先通过验收，再进入下一模块。
